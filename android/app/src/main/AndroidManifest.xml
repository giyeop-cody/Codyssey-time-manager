<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.codyssey.attendance">

    <!-- 권한 (필요 최소한) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 정확한 알람 (알림 앱이 핵심 목적이므로 유지. 미허용 시 WorkManager 경로로 폴리밭) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- W7(18차): 1분 상시 감지 포그라운드 서비스 + 절전모드 예외 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name="kr.codyssey.attendance.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 알림 리시버 (앱 남부 전용: 외부 앱의 위조 브로드캐스트 차단) -->
        <receiver
            android:name="kr.codyssey.attendance.receiver.AlarmReceiver"
            android:exported="false"
            android:enabled="true" />

        <!-- 20차: 1분 감지 부활 알람 수신기 — 백그라운드 서비스 시작 제한 우회용 브로드캐스트 경로 -->
        <receiver
            android:name="kr.codyssey.attendance.receiver.TickReceiver"
            android:exported="false"
            android:enabled="true" />

        <!-- 부팅 완료 시 동기화 재시작 (시스템 브로드캐스트 수신용) -->
        <receiver
            android:name="kr.codyssey.attendance.receiver.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- W7(18차): 1분 상시 감지 포그라운드 서비스 -->
        <service
            android:name="kr.codyssey.attendance.service.PollingService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="학원 출입 처리와 평가 일정을 1분 간격으로 감지해 알람을 울리는 상시 감지 도구" />
        </service>

        <!-- 알림 채널용 프로바이더 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
