<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.codyssey.attendance">

    <!-- 권한 (필요 최소한) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 정확한 알람 (알림 앱이 핵심 목적이므로 유지. 미허용 시 WorkManager 경로로 폴리밭) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- FGS 공통 권한 — 알람 울림 전용 mediaPlayback FGS(25차, 실제 울릴 때만 실행)에 필요 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- 28차: 1분 상시 감지 FGS 폐지 — FOREGROUND_SERVICE_SPECIAL_USE 권한 제거 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- 31차: 물리 탐지(C안) — 위치는 기기 안에서만 계산. SSID/셀/지오펜스 용도 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <!-- 32차 N31-6: 스캔 결과를 위치(학원 근처) 판정에 쓰므로 neverForLocation 선언은
         실제 용도와 모순 — 플래그 제거하고 위치 권한을 함께 요청하는 구조로 정렬 -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name="kr.codyssey.attendance.MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 알림 리시버 (앱 남부 전용: 외부 앱의 위조 브로드캐스트 차단) -->
        <receiver
            android:name="kr.codyssey.attendance.receiver.AlarmReceiver"
            android:exported="false"
            android:enabled="true" />

        <!-- 부팅 완료 시 주기 동기화/알람 복원 (시스템 브로드캐스트 수신용) -->
        <receiver
            android:name="kr.codyssey.attendance.receiver.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- 28차: PollingService/TickReceiver 제거됨 (1분 FGS 폐지).
             구버전 잔재는 MainActivity 초기 실행 시 1회 정리 -->

        <!-- 30차: 5분 감지 틱 수신기 — 브로드캐스트만으로 동작 (FGS·상시 알림 없음) -->
        <receiver
            android:name="kr.codyssey.attendance.receiver.SyncTickReceiver"
            android:exported="false"
            android:enabled="true" />

        <!-- 31차: 물리 탐지 — 지오펜스 이벤트 / 활동 인식 수신기 (앱 남부 전용) -->
        <receiver
            android:name="kr.codyssey.attendance.receiver.GeofenceReceiver"
            android:exported="false"
            android:enabled="true" />
        <receiver
            android:name="kr.codyssey.attendance.receiver.ActivityReceiver"
            android:exported="false"
            android:enabled="true" />

        <!-- 25차: 알람 울림 전담 서비스 (최대 1분 반복 재생 + 해제 고정 알림) -->
        <service
            android:name="kr.codyssey.attendance.service.AlarmSoundService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- 알림 채널용 프로바이더 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
