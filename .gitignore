# Node
node_modules/
npm-debug.log*

# Gradle / Android 빌드 산출물
.gradle/
android/.gradle/
android/app/build/
android/build/
android/capacitor-cordova-android-plugins/build/
local.properties
android/local.properties

# IDE / OS
.idea/
.DS_Store
*.iml

# cap sync 생성물 (web/의 복사본 — 커밋 금지, CI에서 생성)
android/app/src/main/assets/public/
# 48차 (V-1): 키스토어는 리포에 커밋하지 않음 — CI는 secrets에서 복호화
android/app/keystore.jks
android/app/debug.keystore
